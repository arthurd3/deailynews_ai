# The build context is now the repository root, so keep everything the image does
# not need out of it - a smaller context means faster builds and fewer cache misses.
target/
.git/
.github/
.idea/
.vscode/
*.md
!README.md
.env
.env.*
!.env.example
docker-compose.yml
Dockerfile
.dockerignore
.editorconfig
