[%autowidth.stretch, cols="h,a"]
|===
|Description
|Cross-cutting infrastructure every other module is allowed to depend on: cache setup, API documentation, resilience wiring and the shared error vocabulary.  
+
Declared `ApplicationModule.Type` so its nested packages stay reachable from the feature modules. Nothing feature-specific belongs here — a module's own configuration properties live in that module's `internal` package.
|Base package
|`com.arthur.newsbrief.shared`
|Spring components
|_Configuration properties_

* `c.a.n.s.config.CachingProperties` -- Caffeine cache tuning, expressed as native Caffeine spec strings so TTLs can be changed per environment without touching Java.
|===
